# gRPC dependencies
grpcio>=1.60.0
grpcio-tools>=1.60.0
protobuf>=4.25.0

# Configuration
pyyaml>=6.0.1

# Database (SQLite async)
aiosqlite>=0.19.0

# Quantum RNG device support
# Note: Install pyqcc from local wheel file
# pyqcc is not available on PyPI - obtain from Crypta Labs
# Install with: pip install pyqcc-x.y.z-py3-none-any.whl

# Optional: QCicada signature verification
# pyqcicada (if using QCicada devices with signed reads)
